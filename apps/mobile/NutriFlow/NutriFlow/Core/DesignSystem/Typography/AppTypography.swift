import SwiftUI

enum AppTypography {
    static let displayNumber = Font.system(
            size: 50,
            weight: .bold
        )

        static let displayLarge = Font.system(
            size: 34,
            weight: .bold
        )

        static let heading1 = Font.system(
            size: 30,
            weight: .semibold
        )
}
